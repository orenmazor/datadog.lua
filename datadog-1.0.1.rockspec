package = "DataDogLua"
version = "1.0-1"
source = {
   url = "..." -- We don't have one yet
}
description = {
   summary = "Datadog api in lua",
   detailed = [[
     yolo
   ]],
   homepage = "http://...", -- We don't have one yet
   license = "MIT"
}
dependencies = {
   "lua ~> 5.1"
}
build = {
   -- We'll start here.
}
